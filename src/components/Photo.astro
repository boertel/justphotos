---
import { Image } from 'astro:assets';

interface Props {
  id: string;
  src: string;
  alt?: string;
  width: number;
  height: number;
  class?: string;
}

const { id, src, alt, width, height, class: className } = Astro.props;
---

<Image
    class=`rounded-md transition-all ${className}`
    src={src}
    alt={src}
    width={width }
    height={height}
    transition:name=`media-image-${id}`
/>
